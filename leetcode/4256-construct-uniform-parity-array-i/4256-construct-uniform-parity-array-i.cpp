class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        vector<int>nums2;
        return true;
        // for (int i = 0; i < nums1.size(); i++) {
        //     nums2.push_back(nums1[i] - nums[i])
        // }
    }
};