#include <bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--)
  {
    int N;
    cin >> N;
    if (N % 2 == 0) {
      cout << (N / 2) * 30 << endl;
    }
    else {
      cout << (N / 2) * 30 + (N / 3) * 20 << endl;
    }
  }

}
endl local
friend keyword
lower_bound snippet
upper_bound snippet